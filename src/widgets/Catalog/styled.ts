'use client';
import { styled } from 'styled-components';

import { StFlex } from '@/shared/styles/global';

export const StCatalogFlex = styled(StFlex)`
  flex-direction: column;
  align-items: stretch;
  width: 100%;
  gap: 32px;
`;
